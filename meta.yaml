{% set name = "pyprimer-ilps" %}
{% set version = "0.1.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pyprimer-{{ version }}.tar.gz
  # sha256 will be filled in after PyPI upload — run:
  # curl -s https://pypi.org/pypi/pyprimer/{{ version }}/json | python -c "import sys,json; [print(f['digests']['sha256']) for f in json.load(sys.stdin)['urls'] if f['packagetype']=='sdist']"

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps --no-build-isolation -vv

requirements:
  host:
    - python >=3.8
    - pip
    - setuptools
  run:
    - python >=3.8

test:
  imports:
    - pyprimer_ilps
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/RDRidenbaugh/PyPrimer
  summary: "A bioinformatic pipeline for intron length variation-based diagnostic multiplex PCR primer design."
  description: |
    PyPrimer-ILPs identifies intron length variation between multiple species for
    the purpose of designing diagnostic multiplex PCR primers. Originally
    implemented in Neodiprion sawflies, the methodology can be applied to
    any system with an NCBI annotated reference genome.
  license: MIT
  license_family: MIT
  license_file: LICENSE
  doc_url: https://github.com/RDRidenbaugh/PyPrimer
  dev_url: https://github.com/RDRidenbaugh/PyPrimer

extra:
  recipe-maintainers:
    - RDRidenbaugh